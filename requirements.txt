fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
apscheduler>=3.10
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Desktop GUI (gui/) ---
PySide6>=6.7
requests>=2.32

# --- Voice (voice/) - wake word + offline speech, see the Voice section of README.md ---
# All optional: only needed if you're running emma_voice.py.
vosk>=0.3.45          # offline speech recognition (wake word + command STT)
sounddevice>=0.5.5    # microphone capture + speaker playback
numpy>=1.26           # audio buffer math
piper-tts>=1.4        # natural neural feminine voice (offline, CPU) - fallback TTS
pyttsx3>=2.90         # fallback text-to-speech (SAPI5 / NSSpeech / espeak-ng)
# NOTE: the primary Chatterbox-Turbo voice does NOT install here. It needs
# Python 3.11 (the pinned torch has no wheels for this project's newer
# Python), so it lives in its own uv-managed sidecar venv:
#   uv venv .venv-chatterbox --python 3.11
#   uv pip install --python .venv-chatterbox/bin/python chatterbox-tts

# --- Telegram notification bot (notifications/) ---
python-telegram-bot>=22.8
