# Server-only dependencies for headless deployment (HF Space / VM).
# No GUI (PySide6) or voice (vosk/piper/sounddevice) - never imported by main.py.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
apscheduler>=3.10
python-telegram-bot>=22.8
huggingface_hub>=0.24
